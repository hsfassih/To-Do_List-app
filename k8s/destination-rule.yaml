apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: mtls-all
  namespace: todo-app
spec:
  host: "*.todo-app.svc.cluster.local"
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL